Client team rules!

Getting Setup
______________

### Prereqs
Gulp	- `npm install -g gulp`
Bower	- `npm install -g bower`

### Initial Setup
* From `app/`: `bower install`
* From `/`:
	* `npm install -d`
	* `gulp`

